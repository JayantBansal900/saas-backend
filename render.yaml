services:
  - type: web
    name: saas-backend
    env: node
    buildCommand: echo "skipping build on Render"
    startCommand: node dist/main.js
    envVars:
      - key: MONGO_URI
        sync: false
      - key: JWT_SECRET
        sync: false
