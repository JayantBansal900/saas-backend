services:
  - type: web
    name: saas-backend
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm run start:prod
    envVars:
      - key: MONGO_URI
        sync: false
      - key: JWT_SECRET
        sync: false
